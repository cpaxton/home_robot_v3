# emet_lingbot_map

Wrapper installed in `.venv-lingbot-map` alongside the upstream [lingbot-map](https://github.com/Robbyant/lingbot-map) package.

Install via `./scripts/install_lingbot_map.sh` from the repo root.
