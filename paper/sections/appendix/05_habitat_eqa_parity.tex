\section{Habitat HM-EQA harness vs.\ GraphEQA}
\label{app:habitat_eqa_parity}

This appendix records how the \texttt{emet} Habitat EQA harness compares to the reference GraphEQA Habitat stack~\citep{saxena2024grapheqa}, what is intentionally different, and interim accuracy on our development machine.
The harness drives \texttt{GraphEQAMemory} / \texttt{DynagraphController} inside Habitat-Sim rather than ROS + Hydra; HM-EQA uses 113 multiple-choice questions (Explore-EQA indices 0--112) with scene init poses from the public CSVs.
OpenEQA support is a planned harness milestone (phase H in \texttt{docs/plans/HABITAT\_EQA\_HARNESS.md}); not yet wired.

\subsection{Official GraphEQA simulation numbers (HM-EQA)}

Table~\ref{tab:grapheqa_official_hmeqa} reproduces GraphEQA Table~1 (HM-EQA column): success rate on 113 questions, mean VLM planning steps over \emph{successful} trials, and mean trajectory length over successful trials~\citep{saxena2024grapheqa}.
Methods marked $\dagger$ are author reimplementations of Explore-EQA with newer VLMs.

\begin{table}[t]
  \centering
  \caption{GraphEQA paper baselines on HM-EQA (113 questions, Habitat-Sim).}
  \label{tab:grapheqa_official_hmeqa}
  \small
  \begin{tabular}{@{}lccc@{}}
    \toprule
    Method & Success (\%) $\uparrow$ & Planning steps $\downarrow$ & $L_\tau$ (m) $\downarrow$ \\
    \midrule
    Explore-EQA~\citep{ren2024exploreeqa} & 51.7 & 18.7 & 38.1 \\
    Explore-EQA-GPT4o$\dagger$ & 46.4 & 3.4 & 6.3 \\
    Explore-EQA-Gemini-2.5Pro$\dagger$ & 54.3 & 6.40 & 12.3 \\
    SayPlanVision$\dagger$ & 54.8 & 2.6 & 5.3 \\
    \textbf{GraphEQA-GPT4o} & \textbf{63.5} & 5.1 & 12.6 \\
    GraphEQA-Llama4-Mav & 57.7 & 2.36 & 3.59 \\
    \textbf{GraphEQA-Gemini-2.5Pro} & \textbf{67.0} & 2.94 & 7.41 \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent\textbf{Reference setup.}
GraphEQA Habitat sim uses \texttt{use\_semantic\_data: True}: ground-truth HM3D-Semantics instance masks (not Detic) feed Hydra 3DSGs; a TSDF frontier planner with task-relevant frontier enrichment; camera 120$^\circ$ HFOV at 1.5\,m height and $-30^\circ$ tilt (640$\times$480); up to 20 VLM planning iterations; and API VLMs (GPT-4o, Gemini, etc.).
Detic segmentation is used in real-world experiments only.

\subsection{Harness parity checklist}

Table~\ref{tab:habitat_parity} contrasts upstream GraphEQA with our harness as of the Habitat branch.
Items marked \emph{partial} depend on downloaded HM3D semantic assets (only a subset of train scenes ship \texttt{.semantic.glb} in practice).

\begin{table}[t]
  \centering
  \caption{GraphEQA reference vs.\ \texttt{emet} Habitat harness (HM-EQA).}
  \label{tab:habitat_parity}
  \small
  \begin{tabular}{@{}p{0.26\linewidth}p{0.30\linewidth}p{0.14\linewidth}p{0.22\linewidth}@{}}
    \toprule
    Component & GraphEQA (ref.) & Ours & Status \\
    \midrule
    HM-EQA 113-question slice & indices 0--112 & \texttt{--paper-subset} default & Match \\
    Per-question enrich labels & \texttt{explore\_eqa\_dataset\_enrich\_labels.yaml} & bundled YAML + \texttt{seed\_object\_hints} & Match \\
    Camera / agent & 120$^\circ$, 1.5\,m, $-30^\circ$, 640$\times$480 & \texttt{HabitatEQASimulator} & Match \\
    Planning budget & 20 VLM steps, 10 nav substeps & CLI defaults & Match \\
    HM3D GT semantics (sim) & all train scenes w/ data & auto if \texttt{.semantic.glb} exists & \textbf{Partial} \\
    Scene graph & Hydra 5-layer 3DSG & \texttt{GraphEQAMemory} object graph & Different stack \\
    Exploration planner & TSDF + enriched frontiers & fluid voxel \texttt{sample\_frontier} (+ optional graph frontier nodes) & Partial \\
    Navigation execution & navmesh shortest path & \texttt{find\_path\_to\_xy} + trajectory follow; Image-$N$ viewpoint/standoff & Match \\
    MCQ prompt / grading & HM-EQA A--D & \texttt{hmeqa} prompt variant & Match \\
    Graph memory config & static (no merge / staleness) & method \texttt{static\_graph} $\rightarrow$ profile \texttt{static\_graph} (0/0); method \texttt{dynagraph} $\rightarrow$ profile \texttt{unified\_eqa} (0.45\,m / 256) & Match for GE row \\
    Batch logging & per-question JSON & JSONL + \texttt{--resume} & Match \\
    OpenEQA val split & supported & not wired (milestone) & Missing \\
    Detic fallback & real world & N/A in sim harness & N/A \\
    VLM & GPT-4o / Gemini API & local Qwen3-VL-8B (default) & By design \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Semantics and frontier gaps (detail)}

\textbf{Ground-truth semantics.}
When GraphEQA sets \texttt{use\_semantic\_data: True}, every HM-EQA episode is assumed to have HM3D semantic annotations.
Our harness enables the Habitat semantic sensor when \texttt{.semantic.glb} is present for the scene; on a typical cache with train semantics partially downloaded, \textbf{37/113} paper questions and \textbf{14/49} unique paper scenes had semantic assets at the time of writing---the remainder fall back to depth voxel mapping and VLM (or keyword) graph labels.

\textbf{Frontier planning (fluid map vs.\ graph nodes).}
Both DynaMem and GraphEQA share the same \emph{fluid} 2D frontier: reachable free-space edges on the voxel grid (\texttt{get\_outside\_frontier}), minus explored cells.
\texttt{SparseVoxelMapNavigationSpace.sample\_exploration} picks a grid cell using a time-since-visit heuristic and, when \texttt{keyword\_score\_weight}$>0$, a question-keyword overlap map (\texttt{keyword\_score\_map})---no discrete graph required.

\textbf{Frontier v2 (optional, default in \texttt{dynav\_config.yaml}).}
When \texttt{graph\_eqa\_frontier\_nodes.enabled} is true, \texttt{sync\_frontier\_nodes} clusters the same unexplored mask into up to 12 \texttt{GraphEQAMemory} nodes (\texttt{is\_frontier=True}), exposes them in EQA \texttt{IMAGE\_DESCRIPTIONS}, and routes low-confidence iterations via \texttt{\_best\_frontier\_point\_from\_graph} before falling back to fluid \texttt{sample\_frontier}.
HM-EQA CLI flags \texttt{--frontier-nodes}/\texttt{--no-frontier-nodes} and \texttt{--frontier-keyword-weight} select ablation arms (script \texttt{run\_habitat\_frontier\_ablation.sh}).

\textbf{Paper-reproduction baseline.}
Closest to upstream GraphEQA fluid exploration (no graph frontier nodes): \texttt{--no-frontier-nodes --frontier-keyword-weight 0}.
Earlier \texttt{emet} batches that left frontier nodes enabled are \emph{frontier v2}, not a strict paper clone.

Reference GraphEQA still uses a separate TSDF map and VLM \texttt{enrich\_frontiers} prompts; full parity on exploration policy remains future work.
Navigation fixes (2026-06): map VLM \texttt{action: Image $N$} through attached \texttt{obs\_ids} order.
Navigation fixes (2026-07): resolve Image-$N$ to capture viewpoint or standoff waypoint (\texttt{image\_nav\_min\_approach\_m}); block no-op goals; sort frontiers by distance.

\subsection{Interim \texttt{emet} results (development)}

Table~\ref{tab:habitat_interim_results} lists HM-EQA batches on a single RTX~4090 (20 planning steps, 10 nav substeps).
JSONL aggregates and reproduc commands: \texttt{docs/experiments/habitat\_eqa\_results.md}.
Numbers are \emph{not} comparable to Table~\ref{tab:grapheqa_official_hmeqa} until semantics coverage and/or VLM tier match the reference stack.

\begin{table}[t]
  \centering
  \caption{Interim HM-EQA accuracy (\texttt{emet} harness; rescored MCQ parser).}
  \label{tab:habitat_interim_results}
  \small
  \begin{tabular}{@{}llcc@{}}
    \toprule
    Run (slice) & Method / VLM & $n$ & Accuracy \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Full benchmark}} \\
    Paper repro & static\_graph / gemma-3-4b-it & 113 & 41.6\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Letter-balanced 32 (ids in \texttt{habitat\_eqa\_results.md})}} \\
    Balanced-32 & dynagraph / Qwen2.5-VL-3B & 32 & 34.4\% \\
    Balanced-32 & static\_graph / Qwen2.5-VL-3B & 32 & 31.2\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Canonical-8 (3,14,17,28,31,35,81,94)}} \\
    Canonical-8 & dynagraph / Qwen2.5-VL-3B & 8 & 50.0\% \\
    Canonical-8 & static\_graph / Qwen2.5-VL-3B & 8 & 37.5\% \\
    Canonical-8 + debias & dynagraph / Qwen2.5-VL-3B & 8 & 62.5\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Smaller slices (gemma / frontier ablations)}} \\
    Paper repro & static\_graph / gemma-3-4b-it & 10 / 20 & 40\% / 40\% \\
    Q0--9 & Qwen2.5-VL-3B & 10 & 50\% \\
    Q0--19 frontier v2 & gemma4 + graph nodes & 20 & 25\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Post-fix (Qwen3-VL-8B, June 2026)}} \\
    Smoke $\{3,14,17\}$ & dynagraph & 3 & 66.7\% (2/3) \\
    Q17 phrase+location & dynagraph & 1 & 100\% \\
    Held-out random-8 & dynagraph & 8 & 50.0\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{July 2026 nav stack (Qwen3-VL-8B)}} \\
    Smoke $\{3,14,17\}$ & both methods & 3 & 100\% \\
    Held-out random-8 & static\_graph & 8 & 87.5\% \\
    Held-out random-8 & dynagraph & 8 & 37.5\% \\
    Balanced-32 & static\_graph / dynagraph & 32 & 37.5\% / 40.6\% \\
    Paper Q0--19 & static\_graph / dynagraph & 20 & 45.0\% / 50.0\% \\
    \midrule
    \multicolumn{4}{@{}l@{}}{\textit{Tuned harness (pending)}} \\
    Held-out random-8 & dynagraph tuned & 8 & pending \\
    Full paper set & dynagraph / Qwen3-VL-8B & 113 & pending \\
    Full paper set & static\_graph / Qwen3-VL-8B & 113 & pending \\
    Frontier ablation Q0--19 & Qwen2.5-VL-3B & 20 & pending \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent\textbf{Takeaway.}
Official GraphEQA on HM-EQA is \textbf{63.5\%} (GPT-4o) to \textbf{67.0\%} (Gemini-2.5Pro) on all 113 questions with full GT semantics.
Our harness reaches \textbf{41.6\%} on the full set with gemma-3-4b-it ($\sim$22\,pp below GPT-4o) and \textbf{34--50\%} on smaller slices with 3--8B local VLMs---above 25\% chance, with dynagraph consistently ahead of static\_graph on matched runs.
The June 2026 grounding fixes (phrase \texttt{CONFIRMED\_MEMORY}, location MCQ, nav) lift search-style questions (e.g.\ Q17) without obvious overfit on an eight-question held-out draw (50\%).
\textbf{More experiments are required} before claiming parity with Explore-EQA or GraphEQA: at minimum a full 113-question Qwen3-VL-8B sweep and semantics-coverage ablation (see \texttt{docs/experiments/habitat\_eqa\_results.md}).
Mock GraphEQA vs.\ Dynagraph match at 100\% on Q0--5 (grading sanity).
Frontier v2 (graph nodes on) underperformed gemma paper repro on Q0--19 (\textbf{25\%} vs.\ \textbf{40\%}); fluid vs.\ node ablation remains pending.
