# Original-clutter diagnostic: combine the separately tracked contact calibration
# and observed-aperture preparation. Not a replacement for the frozen control,
# and not established on real hardware or across objects.
extends: configs/emet/query_geometry_contact_pilot.yaml
mapping:
  query_memory:
    # Bounded proposal recovery, not a retry of semantic rejection. Frozen
    # detector-only and Qwen-box control presets remain unchanged.
    recover_proposals_with_vlm: true
  grasp:
    observed_aperture_margin_m: 0.06
